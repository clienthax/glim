[package]
name = "stilb_rs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ash = "0.38"
ash-window = "0.13"
glfw-sys = { version = "8", default-features = false, features = ["dynamic-link"] }
